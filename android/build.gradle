allprojects {
    repositories {
        google()
        mavenCentral()
    }
    // dependencies {
    //      implementation 'androidx.appcompat:appcompat:1.1.0'
    // }
}

rootProject.buildDir = "../build"